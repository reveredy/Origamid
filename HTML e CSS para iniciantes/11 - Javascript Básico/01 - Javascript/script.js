const curso = "Javascript";
let curso2 = "HTML";

// A diferença entre const e let é que let nós podemos alterar o valor contido nela, e const que vem de constante não podemos alterar o nome depois de definido

console.log(curso);

console.log(curso2);

curso2 = "CSS";
console.log(curso2);
